diff --git a/Engine/Source/Runtime/Renderer/Private/SceneVisibility.cpp b/Engine/Source/Runtime/Renderer/Private/SceneVisibility.cpp
index 8c7aedb6d98b..e6229ae52d00 100644
--- a/Engine/Source/Runtime/Renderer/Private/SceneVisibility.cpp
+++ b/Engine/Source/Runtime/Renderer/Private/SceneVisibility.cpp
@@ -3821,7 +3821,42 @@ void FSceneRenderer::PreVisibilityFrameSetup(FRDGBuilder& GraphBuilder, const FS
 			View.TemporalJitterPixels.X = SampleX;
 			View.TemporalJitterPixels.Y = SampleY;
 
-			View.ViewMatrices.HackAddTemporalAAProjectionJitter(FVector2D(SampleX * 2.0f / View.ViewRect.Width(), SampleY * -2.0f / View.ViewRect.Height()));
+			static auto CANFVar = IConsoleManager::Get().FindConsoleVariable(TEXT("r.ANF.Enabled"));
+			const bool anfEnabled = CANFVar != nullptr && CANFVar->GetInt() != 0;
+			static auto CANFPatternCVar = IConsoleManager::Get().FindConsoleVariable(TEXT("r.ANF.UseJitterPattern"));
+			const bool anfusePattern = CANFPatternCVar != nullptr && CANFPatternCVar->GetInt() != 0;
+
+			if (anfEnabled && anfusePattern)
+			{
+				auto ANFHalton = [](int32 Index, int32 Base)
+					{
+						Index = (Index + 1) % 16 + 1;
+						float Result = 0.0f;
+						float InvBase = 1.0f / float(Base);
+						float Fraction = InvBase;
+						while (Index > 0)
+						{
+							Result += float(Index % Base) * Fraction;
+							Index /= Base;
+							Fraction *= InvBase;
+						}
+						return Result;
+					};
+
+				int32_t sampleIndex = View.TemporalJitterIndex;
+
+				SampleX = ANFHalton(sampleIndex + 1, 2) - 0.5f;
+				SampleY = ANFHalton(sampleIndex + 1, 3) - 0.5f;
+
+				View.TemporalJitterPixels.X = SampleX;
+				View.TemporalJitterPixels.Y = SampleY;
+
+				View.ViewMatrices.HackAddTemporalAAProjectionJitter(FVector2D(SampleX * -2.0f / View.ViewRect.Width(), SampleY * -2.0f / View.ViewRect.Height()));
+			}
+			else
+			{
+				View.ViewMatrices.HackAddTemporalAAProjectionJitter(FVector2D(SampleX * 2.0f / View.ViewRect.Width(), SampleY * -2.0f / View.ViewRect.Height()));
+			}
 		}
 
 		// Setup a new FPreviousViewInfo from current frame infos.
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/Android/VulkanPlatformDefines.h b/Engine/Source/Runtime/VulkanRHI/Private/Android/AndroidVulkanPlatformDefines.h
similarity index 100%
rename from Engine/Source/Runtime/VulkanRHI/Private/Android/VulkanPlatformDefines.h
rename to Engine/Source/Runtime/VulkanRHI/Private/Android/AndroidVulkanPlatformDefines.h
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/Linux/VulkanPlatformDefines.h b/Engine/Source/Runtime/VulkanRHI/Private/Linux/LinuxVulkanPlatformDefines.h
similarity index 100%
rename from Engine/Source/Runtime/VulkanRHI/Private/Linux/VulkanPlatformDefines.h
rename to Engine/Source/Runtime/VulkanRHI/Private/Linux/LinuxVulkanPlatformDefines.h
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/Mac/VulkanPlatformDefines.h b/Engine/Source/Runtime/VulkanRHI/Private/Mac/MacVulkanPlatformDefines.h
similarity index 100%
rename from Engine/Source/Runtime/VulkanRHI/Private/Mac/VulkanPlatformDefines.h
rename to Engine/Source/Runtime/VulkanRHI/Private/Mac/MacVulkanPlatformDefines.h
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/VulkanBarriers.cpp b/Engine/Source/Runtime/VulkanRHI/Private/VulkanBarriers.cpp
index 1739f370bd9b..66e6ea0b50bf 100644
--- a/Engine/Source/Runtime/VulkanRHI/Private/VulkanBarriers.cpp
+++ b/Engine/Source/Runtime/VulkanRHI/Private/VulkanBarriers.cpp
@@ -734,7 +734,10 @@ void FVulkanDynamicRHI::RHICreateTransition(FRHITransition* Transition, const FR
 		const bool bIsDepthStencil = Texture && Texture->Surface.IsDepthOrStencilAspect();
 
 		// If the device doesn't support separate depth-stencil layouts, we must merge depth-stencil subresource transitions so we only do one barrier on the image.
-		if (bIsDepthStencil && Info.PlaneSlice != FRHISubresourceRange::kAllSubresources)
+		// Copy access on depth/stencil must take the whole-texture path: the subresource collector cannot handle it.
+		const ERHIAccess AnyCopyAccess = ERHIAccess::CopySrc | ERHIAccess::CopyDest;
+		const bool bHasCopyAccess = EnumHasAnyFlags(Info.AccessBefore, AnyCopyAccess) || EnumHasAnyFlags(Info.AccessAfter, AnyCopyAccess);
+		if (bIsDepthStencil && Info.PlaneSlice != FRHISubresourceRange::kAllSubresources && !bHasCopyAccess)
 		{
 			int32 Index = DSSubresTransitions.IndexOfByPredicate([Texture, Info](const FDepthStencilSubresTransition& Entry) -> bool {
 				return Entry.Texture->Surface.Image == Texture->Surface.Image && Entry.ArraySlice == Info.ArraySlice;
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/VulkanContext.h b/Engine/Source/Runtime/VulkanRHI/Private/VulkanContext.h
index cc12ee7cede6..c2c48c500b1c 100644
--- a/Engine/Source/Runtime/VulkanRHI/Private/VulkanContext.h
+++ b/Engine/Source/Runtime/VulkanRHI/Private/VulkanContext.h
@@ -21,7 +21,7 @@ class FVulkanSwapChain;
 
 struct FInputAttachmentData;
 
-class FVulkanCommandListContext : public IRHICommandContext
+class VULKANRHI_API FVulkanCommandListContext : public IRHICommandContext
 {
 public:
 	FVulkanCommandListContext(FVulkanDynamicRHI* InRHI, FVulkanDevice* InDevice, FVulkanQueue* InQueue, FVulkanCommandListContext* InImmediate);
@@ -199,7 +199,7 @@ protected:
 
 	FVulkanCommandBufferManager* CommandBufferManager;
 
-	static VULKANRHI_API FVulkanLayoutManager LayoutManager;
+	static FVulkanLayoutManager LayoutManager;
 
 	FVulkanOcclusionQueryPool* CurrentOcclusionQueryPool = nullptr;
 
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/VulkanPlatform.h b/Engine/Source/Runtime/VulkanRHI/Private/VulkanPlatform.h
index e6f0f8d761a9..c9b1db91c318 100644
--- a/Engine/Source/Runtime/VulkanRHI/Private/VulkanPlatform.h
+++ b/Engine/Source/Runtime/VulkanRHI/Private/VulkanPlatform.h
@@ -3,4 +3,6 @@
 
 #pragma once
 
-#include "VulkanPlatformDefines.h"
\ No newline at end of file
+#include "HAL/PreprocessorHelpers.h"
+#include COMPILED_PLATFORM_HEADER(VulkanPlatformDefines.h)
+
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.cpp b/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.cpp
index 58390242d6fe..52ed5b7bd79b 100644
--- a/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.cpp
+++ b/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.cpp
@@ -12,6 +12,9 @@
 #include "IHeadMountedDisplayModule.h"
 #include "IHeadMountedDisplayVulkanExtensions.h"
 
+ETextureCreateFlags FVulkanSwapChain::sExtraBackbufferUsageFlags = ETextureCreateFlags::None;
+VkImageUsageFlags FVulkanSwapChain::sExtraBackbufferVkUsageFlags = 0x0;
+
 #if PLATFORM_ANDROID
 // this path crashes within libvulkan during vkDestroySwapchainKHR on some versions of Android. See FORT-250079
 int32 GVulkanKeepSwapChain = 0;
@@ -471,6 +474,10 @@ FVulkanSwapChain::FVulkanSwapChain(VkInstance InInstance, FVulkanDevice& InDevic
 	SwapChainInfo.imageExtent.width = SizeX;
 	SwapChainInfo.imageExtent.height = SizeY;
 	SwapChainInfo.imageUsage = VK_IMAGE_USAGE_COLOR_ATTACHMENT_BIT | VK_IMAGE_USAGE_SAMPLED_BIT | VK_IMAGE_USAGE_TRANSFER_DST_BIT;
+	if (GVulkanDelayAcquireImage != EDelayAcquireImageType::DelayAcquire) // App will have direct swapchain image access
+	{
+		SwapChainInfo.imageUsage |= sExtraBackbufferVkUsageFlags;
+	}
 	SwapChainInfo.preTransform = PreTransform;
 	SwapChainInfo.imageArrayLayers = 1;
 	SwapChainInfo.imageSharingMode = VK_SHARING_MODE_EXCLUSIVE;
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.h b/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.h
index 4263e71301dd..3096aa1b546b 100644
--- a/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.h
+++ b/Engine/Source/Runtime/VulkanRHI/Private/VulkanSwapChain.h
@@ -44,6 +44,17 @@ public:
 	const FVulkanTextureView* GetOrCreateQCOMDepthView(const FVulkanSurface& InSurface) const;
 	const FVulkanSurface* GetQCOMDepthStencilSurface() const;
 
+	VULKANRHI_API static inline void SetExtraBackbufferUsageFlags(ETextureCreateFlags extraFlags, VkImageUsageFlags extraVkFlags)
+	{
+		sExtraBackbufferUsageFlags |= extraFlags;
+		sExtraBackbufferVkUsageFlags |= extraVkFlags;
+	}
+
+	VULKANRHI_API static inline ETextureCreateFlags GetExtraBackbufferUsageFlags()
+	{
+		return sExtraBackbufferUsageFlags;
+	}
+
 protected:
 	VkSurfaceTransformFlagBitsKHR QCOMRenderPassTransform = VK_SURFACE_TRANSFORM_IDENTITY_BIT_KHR;
 	VkFormat ImageFormat = VK_FORMAT_UNDEFINED;
@@ -76,6 +87,9 @@ protected:
 
 	uint32 PresentID = 0;
 
+	static ETextureCreateFlags sExtraBackbufferUsageFlags;
+	static VkImageUsageFlags sExtraBackbufferVkUsageFlags;
+
 	int32 AcquireImageIndex(VulkanRHI::FSemaphore** OutSemaphore);
 
 	// WA: if the swapchain pass uses a depth target, it must have same size as the swapchain images.
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/VulkanViewport.cpp b/Engine/Source/Runtime/VulkanRHI/Private/VulkanViewport.cpp
index f23e3414f83b..579fa429f768 100644
--- a/Engine/Source/Runtime/VulkanRHI/Private/VulkanViewport.cpp
+++ b/Engine/Source/Runtime/VulkanRHI/Private/VulkanViewport.cpp
@@ -681,7 +681,13 @@ void FVulkanViewport::CreateSwapchain(FVulkanSwapChainRecreateInfo* RecreateInfo
 		uint32 BackBufferSizeX = RequiresRenderingBackBuffer() ? SizeX : 1;
 		uint32 BackBufferSizeY = RequiresRenderingBackBuffer() ? SizeY : 1;
 
-		RenderingBackBuffer = new FVulkanTexture2D(*Device, PixelFormat, BackBufferSizeX, BackBufferSizeY, 1, 1, TexCreate_RenderTargetable | TexCreate_ShaderResource, ERHIAccess::Present, FRHIResourceCreateInfo(TEXT("RenderingBackBuffer")));
+		ETextureCreateFlags backbufferFlags = ETextureCreateFlags::RenderTargetable | ETextureCreateFlags::ShaderResource | ETextureCreateFlags::ResolveTargetable;
+		if (GVulkanDelayAcquireImage == EDelayAcquireImageType::DelayAcquire)
+		{
+			backbufferFlags |= FVulkanSwapChain::GetExtraBackbufferUsageFlags();
+		}
+
+		RenderingBackBuffer = new FVulkanTexture2D(*Device, PixelFormat, BackBufferSizeX, BackBufferSizeY, 1, 1, backbufferFlags, ERHIAccess::Present, FRHIResourceCreateInfo(TEXT("RenderingBackBuffer")));
 #if VULKAN_ENABLE_DRAW_MARKERS
 		if (Device->GetDebugMarkerSetObjectName())
 		{
diff --git a/Engine/Source/Runtime/VulkanRHI/Private/Windows/VulkanPlatformDefines.h b/Engine/Source/Runtime/VulkanRHI/Private/Windows/WindowsVulkanPlatformDefines.h
similarity index 100%
rename from Engine/Source/Runtime/VulkanRHI/Private/Windows/VulkanPlatformDefines.h
rename to Engine/Source/Runtime/VulkanRHI/Private/Windows/WindowsVulkanPlatformDefines.h
